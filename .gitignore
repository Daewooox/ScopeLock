node_modules/
dist/
*.tsbuildinfo

# ScopeLock: contracts and config are shared, committed artifacts.
# Per-machine working state (reports/, active) is ignored via .scopelock/.gitignore.
.scopelock/reports/
.scopelock/active
